x = 3.14
y = 4
z = 5

result = round(x)  # rounding to the nearest integer
result = abs(y)  # returns the absolute value
result = pow(y, 3)  # returns the first number to the power of second one
result = max(x, y, z)  # returns the maximum value
result = min(x, y, z)  # returns the minimum value

print(x)
